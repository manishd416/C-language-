#include<stdio.h>
void main(){
int a,b,pro;
scanf("%d%d",&a,&b);
int big=(a>b)?a:b;
while(1)
{
    if(big%a==0 && big%b==0){
        break;
    }
    else
    {
        big=big+1;
    }
}

printf("%d",big);
}
