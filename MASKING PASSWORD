#include<stdio.h>
#include<string.h>
void main(){
	int i=0;
	char ch,password[20];
	printf("Enter the password: ");
	while((ch=getch())!=13){
		password[i]=ch;
		i+=1;
		printf(" *");
	}
	password[i]='\0';
	printf("\nEntered password is %s",password);
}
